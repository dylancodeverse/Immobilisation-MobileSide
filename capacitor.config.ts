import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.immobilisation',
  appName: 'immobilisation',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
